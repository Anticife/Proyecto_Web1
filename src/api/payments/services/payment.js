'use strict';

// payments doesn't use a core factory since the controller handles everything directly,
// but Strapi requires a service file for the API to be registered correctly.
module.exports = {};
